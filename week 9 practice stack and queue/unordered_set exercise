#include <iostream>
//include your library
#include <string>

using namespace std;

int main() {
    // Step 1: Define an unordered set to store unique book ISBNs
    

    // Step 2: Prompt the user to enter book ISBNs until they enter "done" 
    cout << "Enter book ISBNs (type 'done' to finish):\n";
    
    // use a loop to collect ISBN numbers
    
        // Check if the user wants to exit
        

        // Check if the book ISBN already exists in the set
       
              // Skip the insertion and prompt for the next ISBN
        

        // Attempt to insert the book ISBN into the unordered set
      

        // Provide feedback to the user based on insertion result
        

    // Step 7: Display the unique book ISBNs entered by the user using a loop
    

    // Step 8: End the program
    return 0;
}
