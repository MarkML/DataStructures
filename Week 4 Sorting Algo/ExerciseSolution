Pseudo-code for Bubble Sort

Procedure BubbleSort(array, size):
    for i from size-1 down to 1:
        for j from 0 to i-1:
            if array[j] > array[j+1]:
                temp <- array[j]   // Use a temporary variable for the swap
                array[j] <- array[j+1]
                array[j+1] <- temp


Pseudo-code for Insertion Sort

Procedure InsertionSort(arr, n):
    for i from 1 to n-1:
        key <- arr[i]
        j <- i - 1
        while j >= 0 and arr[j] > key:
            arr[j + 1] <- arr[j]
            j <- j - 1
        arr[j + 1] <- key
